node_modules
dist
dist-single
tests/screenshots
*.local
tsconfig.tsbuildinfo
dist-apk
android/build

# El APK y el zip se generan; no hace falta versionarlos.
ficha-dnd-*.apk
ficha-dnd-web.zip
ficha-dnd.html

# La clave de firma NO va al repositorio: GitHub Pages gratis obliga a que sea
# público, y con la clave cualquiera podría firmar una actualización falsa.
# Guárdala fuera, y sin perderla: sin ella no puedes actualizar la app.
android/*.keystore
